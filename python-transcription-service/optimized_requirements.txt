# 优化的依赖列表 - Railway部署版本
# 核心Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 音频处理（核心依赖）
openai-whisper==20231117
torch==2.1.0
numpy==1.24.3
pydub==0.25.1

# 数据库
pymongo==4.6.0

# 其他工具
requests==2.31.0
aiofiles==23.2.1
psutil==5.9.6

# 移除的依赖（以节省内存）：
# - redis（不需要缓存层）
# - websockets（暂时不需要实时功能）
# - speech-recognition（使用Whisper替代）